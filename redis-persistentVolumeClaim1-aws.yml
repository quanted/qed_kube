apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-pvolume-claim1
  annotations:
    volume.beta.kubernetes.io/storage-class: "aws-efs"
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hms-dotnetcore-appdata-pvolume-claim1
  annotations:
    volume.beta.kubernetes.io/storage-class: "aws-efs"
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi

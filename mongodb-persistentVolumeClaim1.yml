apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-pvolume-claim1
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: mongodb-pv1
